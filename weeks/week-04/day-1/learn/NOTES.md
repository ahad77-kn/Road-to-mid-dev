# Notes — Week 04 Day 1: Variables, types, operators

**Date:** 2026-08-17
**Time spent:** learning 10am to 11am · building 11am to 3pm

**AI used?** yes yes / no — what for: used for learning about js start up and about whole assignment and got help of ai because i was not getting up.
> Mandatory. Explaining a concept = fine, full marks. Writing your code = say so.
> Not saying so = 0/10. → [the rule](../../../../GRADING.md)

## What I learned (own words, min 3)

-learned variables,if/else conditions.

-learn about js,const and let values ,inwchich conts is for value which cannot be cahnged and if for value can be changed.

-learned about the "typeof null" which gives object instead of its value is null.


## What I struggled with (mandatory — "nothing" is not allowed)

-i struggled to learn and write it by my self but was my first time so ...

## How I solved it (or: still stuck, will ask)

-so i got help of ai.


## The teacher's questions — my answers, from memory

1. Difference between let, const and var — in scope, and in reassignment?
ans. let → block-scoped, can reassign. const → block-scoped, cannot reassign. var → function-scoped, can reassign.

2. What are the 7 primitive types in JavaScript?
ans. string, number, bigint, boolean, undefined, symbol, null

3. What does typeof null return, and why is it wrong?
typeof null → "object". It's a historical JavaScript bug.

4. What is the difference between null and undefined — when would you deliberately use each?
undefined → value not assigned. null → deliberately saying “no value”.

5. Prove it: predict the output of console.log("5" + 3, "5" - 3, 5 + null, "5" == 5, "5" === 5) before running it. Then run it.
ans. Predicted output:
"53" 2 5 true false

Because + joins strings, - converts to numbers, null becomes 0, == allows conversion, and === doesn't.

## One question I still have

-i learned all the topics of today but nothing was of topic to make me able of make the today assignment.
