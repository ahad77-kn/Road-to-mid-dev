## the refactor notes
## the main things that better

- The code became easier to reuse because each task is now inside a function that can be called whenever I need it.

- code become easier to reuse because related functions are grouped into "math.js","string.js" and pattern.js.

- the main.js file is much cleaner because it only calls the function and displays the results instead of containing all the logic.

## function i struggle to name 

- i struggle to nsme the function that finds the largest number because there are sevral possible names such as `largest`, `findLargest`, and `getLargestNumber`.

- I chose `findLargest` because the function takes a list of numbers and searches through it to find the largest one.