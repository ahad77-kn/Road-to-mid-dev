# 🧪 Daily test — Week 02 Day 5

**Closed book. No editor, no browser, no AI, no notes. 20 minutes** — this one is
the week's wrap-up and it is longer.

Answers in `ANSWERS.md`, then self-mark at the bottom.

---

## Section A — recall (4 marks, 1 each)

**A1.** Grid is two-dimensional, Flexbox is one-dimensional. Give a concrete layout
that is genuinely painful in Flexbox and easy in Grid, and say why in one sentence.

**A2.** What is the default value of `min-width` on a flex item, and why does it cause
overflow?

**A3.** Name the three properties you need together to get a single line of text to
truncate with an ellipsis.

**A4.** What is the difference between `justify-content` and `justify-items` in Grid?

## Section B — predict the output (3 marks, 1 each)

**B1.** A grid item contains one unbreakable 400-character word. The column is
`1fr`. What happens to the layout, and what one declaration fixes it?

**B2.**
```css
.parent { display: block; gap: 1rem; }
```
What does the `gap` do here?

**B3.** You set `position: absolute` on a child without setting `position` on any
ancestor. What is that child positioned relative to?

## Section C — spot the bug (2 marks, 1 each)

**C1.** `text-overflow: ellipsis` is set and no ellipsis appears. Give the two most
likely reasons.

**C2.** A card layout uses `position: absolute; top: 214px; left: 268px` to place a
label. State which [standing rule](../../../../STANDING-RULES.md) this breaks and what
should have been used.

## Section D — defend a decision (1 mark)

**D1.** You built this week's YouTube skeleton with Grid for the page and Flexbox for
the components. Someone says "just use Grid for everything, it can do both." Give
your strongest counter-argument — and then the strongest argument *for* their
position.

---

## ✍️ Your self-mark

| Section | Max | Mine |
| --- | --- | --- |
| A — recall | 4 | |
| B — predict | 3 | |
| C — spot the bug | 2 | |
| D — defend | 1 | |
| **Total** | **10** | |

**Honesty bonus:** within 1.0 of my mark → **+0.5** on the day. More than 2.0
optimistic → −0.5.
